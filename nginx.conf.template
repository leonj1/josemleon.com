server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /metrics {
        proxy_pass http://${INGEST_HOST}:${INGEST_PORT}/metrics;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
